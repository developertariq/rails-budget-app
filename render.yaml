databases:
  - name: mysite
    databaseName: mysite
    user: mysite

services:
  - type: web
    name: tariq-budget-app
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite
          property: connectionString
      - key: 535c63b252c0a600b6bf4ef6e40b5059
        sync: false